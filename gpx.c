#include "gpx.h"

bool gpx_start_recording(GpsStatus* status) {
    UNUSED(status);
    return true;
}

void gpx_stop_recording() {
}

bool gpx_update(GpsStatus* status) {
    UNUSED(status);
    return true;
}
